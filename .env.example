# This site is fully static — all content lives in src/data/* and no
# environment variables are required to run or build it.
#
# The contact form posts to /api/contact, which currently just logs the
# submission. If you wire it up to an email/webhook provider, add the
# relevant keys here.
